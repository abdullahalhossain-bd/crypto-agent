# Copy to .env and fill in — never commit the real .env file.
# config_loader.py reads MT5_PASSWORD from the environment; --mode=live
# refuses to start without it (a cached terminal login could silently
# connect to the wrong account).

MT5_PASSWORD=
